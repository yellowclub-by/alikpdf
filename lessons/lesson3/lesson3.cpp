#include <iostream>
using namespace std;


int main()
{



    /*операторы сравнения сравнивают значения показывают true или false
    логические операторы соеденяют несколько условий
    операторы присваивания изменяют значения переменной*/
    /*операторы сравнения
    ==
    !=
    >
    <
    >=
    <=*/
    /*int a = 7;
    int b = 10;
    cout << (a == b) << endl;
    cout << (a != b) << endl;
    cout << (a > b) << endl;
    cout << (a < b) << endl;
    cout << (a >= b) << endl;
    cout << (a <= b) << endl;*/
    /*логическиее операторы:
    && логичееское и AND рфботает только если оба сравнения дает true если хотя бы одно false то false
     ||если одно true то будет выводится 1
     ! логическое не (NOT) перевор true=false,false=true */
     /*cout << (5 > 3 && 10 > 7) << endl;
     cout << (5 > 3 && 10 < 7) << endl;
     cout << (2 > 3 && 10 > 7) << endl;
     cout << !(5 > 4) << endl;*/
     /*int a, b, c;
     cin >> a>>b>>c;
     cout << (a != b && b != c && a != c);*/
     /*операторы присваивания
     Для записи или изменения знач переменной*/
     /*int x = 10;
     x += 5;
     cout << x << endl;
     x -= 2;
     cout << x << endl;
     x *= 2;
     cout << x << endl;
     x /= 2;
     cout << x << endl;
     x %= 2;
     cout << x << endl;*/
     /*int a = 2;
     int b =2;
     int c = 5;
     cout << ((a % 2 == 0 && b % 2 != 0
         && c % 2 != 0) || (a % 2 != 0 && b % 2 == 0 && c % 2 != 0) || (a % 2 != 0 && b % 2 != 0 && c % 2 == 0))<< endl;*/
         /*int a;
         cin >> a;
         cout<< (a % 2 == 0 && a > 0);*/
         /*  int a = 4;
             int b = 2;
             cout << (a % 2 == 0 && b % 2 == 0)<<endl;*/
    //int a = 4;
    //int b = -4;
    //int c = 5;
    //cout << (a + b + c > 0) << endl;
    //cout << (a > 0 && b > 0 && c > 0) << endl;



}